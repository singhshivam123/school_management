<div class="content-wrapper">
  <div class="page-header">
    <h3 class="page-title"> Standard Tables </h3>
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="<%= teachers_standards_path %>">Standard Tables</a></li>
        <li class="breadcrumb-item active" aria-current="page">All Standard</li>
      </ol>
    </nav>
  </div>
  <div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
      <div class="card">
        <div class="card-body">
          <h4 class="card-title">Standard table</h4>
          <table class="table table-bordered table-responsive">
            <thead>
              <tr>
                <th>Name</th>
                <th>Description</th>
                <th>Board</th>
                <th>Show</th>
              </tr>
            </thead>

            <tbody>
              <% @standards.each do |standard| %>
              <tr>
                <td><%= standard.name %></td>
                <td><%= standard.description %></td>
                <td><%= standard.board %></td>
                <td><%= button_to "show", teachers_standard_path(standard), method: :get, class: "btn btn-gradient-info btn-fw"  %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
