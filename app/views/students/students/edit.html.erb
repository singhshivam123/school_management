<div class="content-wrapper">
  <div class="page-header">
    <h3 class="page-title"> Editing Student </h3>
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="<%= students_students_path %>">Student Tables</a></li>
        <li class="breadcrumb-item active" aria-current="page">Edit Student Details</li>
      </ol>
    </nav>
  </div>

  <%= render 'form', student: @student %>

  <%= link_to 'Show', students_student_path(@student) %> |
  <%= link_to 'Back', students_students_path %>
</div>
